<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.anew.frcscoutingapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainMenu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ScouterMenu" />
        <activity android:name=".PreMatchMenu" />
        <activity android:name=".PostMatchMenu" />
        <activity android:name=".QRStager" />
        <activity android:name=".QRDeployer" />
        <activity android:name=".MasterMenu" />
        <activity android:name=".MasterScanner" />
        <activity android:name=".MasterStager" />
        <activity android:name=".SettingsMenu" />
        <activity android:name=".AutonMenu" />
        <activity android:name=".TeleopMenu" />
        <activity android:name=".TeleopMenu2" />
        <activity android:name=".AutonMenu2" />
    </application>

</manifest>